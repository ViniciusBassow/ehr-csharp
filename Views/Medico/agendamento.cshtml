@model ehr_csharp.Models.Medico

@{
    ViewData["Title"] = "configuracao";
}


<html lang="en">
<body>
    <div class="container mt-4">
        <div class="header">
            <h1>Scheduling</h1>
            <button class="btn btn-primary">Add a New Appointment</button>
        </div>

        <div class="tabs mt-3">
            <div class="tab active">Today Appointments</div>
            <div class="tab">Week Appointments</div>
            <div class="tab">Month Appointments</div>
        </div>

        <div class="filter mt-3">
            <label for="filter">Filter by</label>
            <select id="filter" class="form-select">
                <option value="">Select</option>
            </select>

            <label for="select-date" class="ms-3">Select Date</label>
            <input type="date" id="select-date" class="form-control" value="2020-05-28">
        </div>

        <table class="table table-hover mt-3">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Status</th>
                    <th>Patient Name</th>
                    <th>Date</th>
                    <th>Provider</th>
                    <th>Type</th>
                    <th>Chief Complaint</th>
                    <th>Confirmation</th>
                    <th>Copay</th>
                    <th>Eligibility</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
               @*  @foreach (var agendamento in Model)
                {
                    <tr>
                        <td>@agendamento.Id</td>
                        <td class="status-@agendamento.Status.ToLower()">@agendamento.Status</td>
                        <td>@agendamento.PatientName</td>
                        <td>@agendamento.Date.ToString("MM/dd/yyyy hh:mm tt")</td>
                        <td>@agendamento.Provider</td>
                        <td>@agendamento.Type</td>
                        <td>@agendamento.ChiefComplaint</td>
                        <td class="confirmation-@agendamento.Confirmation.ToLower()">@agendamento.Confirmation</td>
                        <td class="copay-@agendamento.CopayStatus.ToLower()">@agendamento.CopayStatus</td>
                        <td>@agendamento.Eligibility</td>
                        <td class="actions">
                            <i class="view">👁️</i>
                            <i class="edit">✏️</i>
                            <i class="delete">🗑️</i>
                        </td>
                    </tr>
                } *@
            </tbody>
        </table>
    </div>
    @* <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" integrity="sha384-oBqDVmMz4fnFO9gybUgGz9pG6aF7pS26O3pO+K3cW9KvQKfN9B+zdB9czb4oZg6F6" crossorigin="anonymous"></script> *@
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0A1ZQ0PbP6hg/yO3zD27HCBzqP3lFZr5T6jZr2j9+/dI5OnZ" crossorigin="anonymous"></script>
</body>
</html>