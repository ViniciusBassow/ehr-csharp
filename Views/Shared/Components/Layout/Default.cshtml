@model Usuario

<div>
    @if (Model != null)
    {
        <div class="dropdown">
            @* <a class="avatar avatar-sm text-bg-dark rounded-circle" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="false" aria-expanded="false"><img src="@Model.UserImageBase64" alt="Imagem do usuário" /></a> *@
            <div class="avatar avatar-sm text-bg-dark rounded-circle overflow-hidden" role="button" data-bs-toggle="dropdown" aria-haspopup="false" aria-expanded="false">
                @(!string.IsNullOrEmpty(@Model.UserImageBase64) ? Html.Raw($"<img src='{@Model.UserImageBase64}' class='w-100 h-100 avatar avatar-sm text-bg-dark rounded-circle overflow-hidden d-inline-block'  style='object-fit: cover; ' />") : Html.Raw("<i class='bi bi-file-fill'></i>"))
            </div>

            <div class="dropdown-menu dropdown-menu-end">

                <button type="submit" class="dropdown-item" href="#"><i class="bi bi-eraser"></i> Trocar senha</button>
                <form asp-action="LogOff" asp-controller="Usuario" method="post">
                <button type="submit" class="dropdown-item" href="#"><i class="bi bi-box-arrow-left"> </i> Sair</button>
                </form>

            </div>
        </div>
        // Adicione outras informações que você deseja exibir
    }
    else
    {
        
    }
</div>
